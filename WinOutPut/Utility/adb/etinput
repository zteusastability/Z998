# Script to start "input" on the device, which has a very rudimentary
# shell.
#
base=/data
export CLASSPATH=$base/etinput.jar
exec app_process /system/bin $base com.android.commands.etinput.EtInput $*

